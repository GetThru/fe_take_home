const initialState = {
  "1": {
    id: "1",
    text: "How are you doing today?",
    choices: ["Great", "Fine", "Not good", "Very bad"]
  }
};

const reducer = (state = initialState, action) => {
  return state;
};

export default reducer;
